# 全球人才精准匹配系统 · 系统开发方案

> 定位：在已有数据底座（2000万论文 + 1800万专家名录 → 统一人才库 + 合著关系网）之上，构建**对外产品**。
> 复用：`deployment_plan.md`（数据底座 P0–P9）、`relationship_network.md`（合著图）。本文是产品层开发方案。
> 关键转变：公司电脑只做开发/样本验证；**带注册登录和支付的多用户产品必须跑在正式服务器（Linux 服务器/云）+ 域名 + HTTPS**，不能停在桌面 WSL2。

---

## 一、功能清单

### 1.1 你提出的 5 项（MVP 必须）
1. **自然语言检索人才** —— 复用检索/匹配引擎（P6–P7）。
2. **上传企业简介 / 职位需求 → 批量匹配人才列表** —— JD 经 LLM 抽取结构化需求，对人才库做混合检索，排序输出。
3. **用户注册 / 登录** —— 账号体系 + 多租户（一个公司多个顾问账号）。
4. **支付模块** —— 按月订阅 vs 按条付费，见第四节专项分析。
5. **单人 × 单企业深度匹配报告** —— 多维度解释"为什么匹配"（第五节）。

### 1.2 我补充的、重要但你没提到的功能
> 这些决定产品能不能真正商业化、能不能留住客户、能不能合法运营。

**A. 人才详情 / 360° 画像**
- 点开一个人才，看完整履历：研究方向、论文列表、引用/h-index、职业时间线、**合著关系网可视化**（复用 P3.5）。
- "找相似人才"：基于 embedding 的近邻检索（已有能力，直接做入口）。

**B. 企业库 / 职位库管理**
- 企业简介、历史职位需求可保存复用；支持"用上次的需求再匹配一批"。

**C. 人才池 / 收藏 / 标签 / 团队协作（RBAC）**
- 把匹配结果存入 pipeline（人才池），打标签、写备注、分配负责人。
- 角色：老板/管理员、招聘顾问、只读访客；多账号协作是 ToB 刚需。

**D. 匹配度评分与可解释（列表级）**
- 每条结果除分数外，给简短"匹配理由"（如：研究方向契合 92%、近 3 年顶会 5 篇、与贵司合作者同校）——降低用户决策成本。

**E. 触达与合规（最重要的一块，你没提但绕不开）**
- **联系方式分级授权**：邮箱/电话属 PII，不能直接对免费用户开放；按付费层级/单独购买解锁，且需脱敏展示。
- **出站触达模板**：生成 cold outreach 邮件/话术，方便顾问直接联系。
- **合规框架**：见第六节（PIPL / GDPR / 数据授权）。

**F. 账单 / 用量中心 + 发票**
- 实时看已用检索次数、已解锁条数、已生成报告数；订阅到期提醒；发票申请。

**G. 人才预警（匹配订阅）**
- "符合某职位的新入库人才"主动推送（数据增量更新时触发），把产品从"人找"变成"人找你"。

**H. 多语言**
- 全球人才 → UI 与检索至少支持中/英；embedding 用 BGE-M3 已天然多语。

**I. 数据增量更新管线**
- 3800万库如何保持新鲜（定期增量导入新论文/新专家），否则产品价值衰减。

**J. 反馈闭环**
- 用户对某条匹配标"好/差" → 沉淀为正负样本，反哺排序模型（类似 P4 的 ML 匹配）。

**K. 导出**
- 人才列表导出 Excel/PDF，供离线汇报客户。

**L. 开放 API**
- 企业客户把匹配能力嵌进自己的 ATS/HR 系统。

**M. 安全**
- 传输/存储加密、操作审计日志、防爬限流、密钥管理。

---

## 二、系统架构

（架构图见下方可视化组件）

分层：
- **接入层**：Nginx + HTTPS + 域名；API 限速/鉴权网关。
- **业务层（FastAPI 微服务）**：认证、检索、匹配、报告、计费、通知。
- **数据层**：PostgreSQL + pgvector（persons / users / companies / job_reqs / reports / billing）、Neo4j（合著网络）、对象存储（上传 JD、报告 PDF）。
- **外部**：LLM（Qwen-Max / DeepSeek / GPT 代理）、支付网关（支付宝/微信/Stripe）、邮件/短信。

**技术栈**

| 层 | 选型 |
|----|------|
| 前端 | React + TypeScript（Next.js 或 Vite）；MVP 先用 Streamlit 验证检索，再产品化 |
| 后端 | Python + FastAPI（检索/匹配/报告/计费） |
| 数据库 | PostgreSQL 16 + pgvector |
| 图库 | Neo4j（深度关系分析，按需） |
| 向量 | BGE-M3（modelscope 拉取） |
| 认证 | JWT + bcrypt；或 Supabase Auth 加速 |
| 支付 | 支付宝/微信支付（国内）+ Stripe（海外） |
| 部署 | Docker Compose（单机）→ K8s/云（生产）；公司机仅做开发 |

---

## 三、数据模型扩展（在 persons 之上新增）

```sql
-- 账号与租户
users(id, email, password_hash, role, tenant_id, created_at)
tenants(id, name, plan, quota_search, quota_detail, quota_report)

-- 企业 / 职位
companies(id, tenant_id, name, profile_text, created_at)
job_reqs(id, company_id, title, req_text, structured_filters JSONB, created_at)

-- 匹配与人才池
pipeline(id, tenant_id, job_req_id, person_id, match_score, match_reason, status, owner_id)
match_reports(id, person_id, company_id, report_markdown, dimensions JSONB, created_at)

-- 计费
subscriptions(id, tenant_id, plan, status, start_at, end_at)
payments(id, tenant_id, amount, channel, status, webhook_payload JSONB)
usage_logs(id, tenant_id, action, count, at)   -- 检索/解锁/报告 都记一笔

-- 反馈与预警
match_feedback(id, person_id, job_req_id, label, at)
talent_alerts(id, job_req_id, person_id, notified_at)
```

---

## 四、支付与计费专项（你问哪个更划算）

### 4.1 对比

| 维度 | 按月订阅 | 按条付费 |
|------|----------|----------|
| 收入可预期 | 高（稳定现金流，利于融资/排期） | 低（随使用波动） |
| 重度用户成本 | 低（包月划算，敢随便搜） | 高（容易超预算，且"摸索式检索"会疯狂计费） |
| 轻度/偶发用户 | 可能浪费（用不满） | 划算（用多少付多少） |
| 平台边际成本 | 需限速/配额防滥用 | 可直接转嫁（每条约等于 LLM+检索成本） |
| 用户体验 | 敢探索，粘性高 | 每次操作都犹豫，易流失 |
| 适用对象 | 核心留存客户 | 长尾/试用客户 |

### 4.2 推荐：**混合模型（订阅为主 + 深度报告按份 + 免费层转化）**
理由：检索/匹配是高频、低边际成本动作，包进订阅能鼓励使用、养出习惯；深度报告是低频、高价值、高 token 成本动作，按份收能覆盖 LLM 成本且不影响订阅粘性。

**定价示例（落地时按成本校准）：**
| 档位 | 价格 | 含 | 适合 |
|------|------|----|------|
| 免费层 | ¥0 | 20 次检索 + 看 5 条基础详情 | 注册转化漏斗 |
| 基础版 | ¥999/月 | 200 次检索 + 50 条详情 + 2 份深度报告 | 个人顾问 |
| 专业版 | ¥2999/月 | 无限检索 + 500 条详情 + 10 份报告 | 小团队 |
| 企业版 | 定制 | 多账号 + API + 不限 | 猎头公司/HR 部门 |

- **深度报告单独按份**：¥49–99/份（覆盖 LLM 成本 + 毛利），超出订阅配额后自动按份。
- **（可选）解锁完整联系方式按条**：¥9–19/条，或计入订阅配额。

### 4.3 算账示例（说明为什么混合最稳）
- 重度用户月看 300 详情 + 10 报告：纯按条 ≈ 300×¥15 + 10×¥80 = ¥5300；专业订阅 ¥2999 含 500 详情+10 报告 → **省 ¥2300，订阅划算**。
- 偶发用户月看 10 详情：按条 ≈ ¥150；订阅 ¥999 → **按条/免费层更划算**。
- 结论：用免费层+按条承接长尾，用订阅锁住核心，两边都不丢。

### 4.4 支付集成
- 国内：支付宝开放平台 / 微信支付（需企业资质、商户号）。
- 海外：Stripe（信用卡）。
- 流程：创建订单 → 调支付 → **webhook 回调更新 subscriptions/payments 状态** → 开通权益。务必处理"支付成功但 webhook 迟到"的幂等。

---

## 五、深度匹配报告（功能 5）

**输入**：人才画像（论文/引用/网络/履历）+ 企业需求（JD/公司简介）。
**生成方式**：RAG（检索该人才相关论文/合作者）+ LLM 长文生成，结构化多维度输出：

1. **研究方向契合度**：人才领域 vs 企业需求，重合度与互补点。
2. **学术成果与影响力**：引用、h-index、顶刊/顶会、代表性工作。
3. **职业阶段与可流动性**：青千/优青潜力、是否处于可流动窗口、地域偏好。
4. **合作网络协同**：与企业已有合作者/同校的关系（复用 P3.5），落地桥梁。
5. **地域与落地可行性**：来华/落地意愿信号、政策匹配（结合你的人才政策自媒体数据）。
6. **风险与注意**：信息缺失、潜在冲突、需进一步核实项。

每维度给评分 + 依据（带引用，如"基于其 2024 年 X 论文"）。报告存 `match_reports`，可导出 PDF。

**成本**：单次报告约数千–上万 token，故按份计费覆盖。

---

## 六、合规与风险（重点，务必前置）

> 3800万人的个人信息 + 商业化收费，必然触发监管。合规不是可选项。

- **数据来源合法性**：期刊论文元数据多为公开，但**专家名录库的商用授权**必须审查（是否有转售/商用条款）；建议法务确认或只商业化已获授权部分。
- **个人信息保护法（PIPL）/ GDPR**：涉及欧盟个人需 GDPR。落实：目的限定、最小必要、单独同意（商业化需告知并取得同意）、个人行使查阅/更正/删除权（提供"被遗忘"入口）。
- **联系方式脱敏与分级**：邮箱/电话默认脱敏，仅对付费且通过合规审查的账号分级开放。
- **平台安全**：传输 HTTPS、存储加密、密钥托管、操作审计、防爬限流、定期渗透测试。
- **内容合规**：生成的报告/话术不得含歧视性表述（年龄、性别、地域歧视）。

---

## 七、开发阶段路线图

| 阶段 | 内容 | 主要产出 | 建议工期 | 执行人 |
|------|------|----------|----------|--------|
| 1 | 数据底座（已有 P0–P5） | 统一人才库 + 向量 + 合著图 | 已完成/进行中 | 实习生 |
| 2 | 检索/匹配 API | /search、/match(JD→人才) | 1–2 周 | 实习生 |
| 3 | 用户认证 + RBAC + 多租户 | users/tenants、登录 | 1 周 | 实习生 |
| 4 | 企业/职位管理 + 匹配流水线 | 上传 JD→列表→人才池 | 1–2 周 | 实习生 |
| 5 | 深度匹配报告 | /report 多维度生成 | 1 周 | 老板+实习生 |
| 6 | 支付与账单 | 订阅/按条 + webhook + 用量 | 1–2 周 | 实习生+老板(资质) |
| 7 | 前端产品化 | React：登录/检索/匹配/报告/账单 | 2–3 周 | 实习生 |
| 8 | 合规·安全·上线 | HTTPS/域名/加密/审计/隐私入口 | 1–2 周 | 老板+外包/顾问 |
| 9 | 运营功能 | 预警/反馈/API/导出/多语言 | 持续 | 实习生 |

**MVP 建议范围（先上线验证收费）**：自然语言检索 + JD 批量匹配 + 注册登录 + 基础付费（免费层+专业版订阅）+ 深度报告按份。其余（Neo4j 深度图、API、多语言）二期。

---

## 八、团队与角色

- **老板**：架构与定价拍板、支付/合规资质、验收。
- **实习生**：按阶段写脚本/接口/前端，跟我逐条执行。
- **可选外包**：支付对接、安全审计、合规文书（老板把关）。

---

## 九、待你确认的关键决策

1. **部署目标**：产品正式跑在哪台服务器/云？（公司机只做开发）
2. **支付渠道**：先接国内（支付宝/微信）还是也要 Stripe 海外？
3. **合规策略**：专家名录能否商用？是否先只用论文库公开部分上线？
4. **首期范围**：是否按第九节的 MVP 范围先上？

---

## 十、下一步

确认上述 4 项后，我从**阶段 2（检索/匹配 API）**开始给实习生派具体任务；同时可以并行起草数据模型扩展的建表脚本。你也可以先决定定价档位，我据此把计费表结构定稿。
