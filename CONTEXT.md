# Relationship Network

全球人才精准匹配平台。多租户 SaaS:租户通过成员协作管理企业与职位匹配,平台按套餐计费。

## Language

### 领域

**租户(Tenant)**:
平台的计费与权限边界。所有成员、企业、订单都归属于一个租户。
_Avoid_: 工作区、组织、团队

**成员(Member)**:
隶属于某个租户的用户,持有该租户内的角色与权限。
_Avoid_: 账户、员工

**企业(Company)**:
租户维护的用人方档案,是职位与匹配业务的挂载点。
_Avoid_: 公司实体、客户

**职位(Job)**:
企业发布的用人需求档案,归属于某个企业,具有草稿、启用、关闭等状态,是活跃职位额度的计量对象。
_Avoid_: 岗位、需求、JD

**职位材料(Job material)**:
以 PDF、DOCX 或 TXT 形式上传的职位说明文件,归属于某个职位。
_Avoid_: 附件、职位描述文件

**职位需求输入快照(Job requirement input snapshot)**:
某次职位需求解析提交时冻结的职位描述、选定职位材料及用户修正文本,后续来源变化不改变该快照。
_Avoid_: 完整提示词、临时拼接文本

**来源冲突(Source conflict)**:
不同职位需求输入来源对同一职位条件给出不能同时成立的内容,必须由用户解决后才能确认职位需求版本。
_Avoid_: 模型歧义、解析警告

**职位需求解析任务(Job requirement parsing task)**:
将一份职位需求输入快照解析为职位需求草稿的持久化工作单元,具有排队、执行、等待重试、取消中、成功、失败和已取消状态。
_Avoid_: 解析请求、AI 任务

**职位需求草稿(Job requirement draft)**:
用户尚未确认、可以修改的结构化职位条件,不能作为匹配依据。
_Avoid_: JD 解析结果、解析草稿

**职位需求版本(Job requirement version)**:
用户确认后的不可变职位条件快照,是匹配运行引用的需求依据。
_Avoid_: JD 版本、职位版本

**可执行条件(Executable condition)**:
使用平台固定字段与操作符表达、能够实际参与人才匹配的职位条件。
_Avoid_: 动态筛选字段、模型筛选条件

**硬条件(Hard condition)**:
不满足时会将人才排除在候选范围之外的可执行条件。
_Avoid_: 必须项、淘汰条件

**偏好条件(Preference condition)**:
不排除人才、仅参与确定性评分的可执行条件。
_Avoid_: 软条件、模型偏好

**研究主题查询(Research topic query)**:
表达职位目标研究方向、技术主题和领域语义的可编辑查询,用于论文向量召回和相关性评分,不属于硬条件。
_Avoid_: 研究关键词、专业硬条件

**职位需求 Schema 版本(Job requirement schema version)**:
共同冻结可执行条件目录、允许的操作符、结构化输出形态和业务校验规则的不可变契约版本。
_Avoid_: JSON 版本、字段版本、检索契约版本

**未支持条件(Unsupported condition)**:
已从职位材料中识别但不在可执行条件目录内的职位条件,保留并展示但不参与匹配。
_Avoid_: 无法满足条件、人工复核项

**检索底座(Search base)**:
本商业产品之外、提供人物与论文检索结果的独立系统。只有本产品的服务端消费其对外契约;租户成员与平台管理员的个人身份不能直连,本产品也不把它的内部存储当作本系统数据。
_Avoid_: CombineDatabase、搜索引擎、OpenSearch、数据底座

**规范人物(Canonical person)**:
检索底座对其聚类合并后的人物给出的主身份。同一规范人物可对应多个当前与历史来源标识,不是本商业产品为租户保存的人才记录。
_Avoid_: 本地人才、person、专家、学者、作者出现

**数据版本(Data version)**:
检索底座一次已校验发布的人物与论文索引的标识。检索底座只提供当前数据版本的读取,一次成功读取中的规范人物、论文与证据同属该标识。
_Avoid_: 快照标识、索引版本、数据更新批次、历史索引

**检索契约版本(Search contract version)**:
检索底座对外读取能力的不可变契约版本,冻结规范人物与人物证据的形状、数据版本与当前缺失的表达,并声明可执行的职位需求 Schema 版本。与数据版本独立。
_Avoid_: API 版本、接口版本、OpenSearch 映射版本、职位需求 Schema 版本

**当前缺失(Absent from current data version)**:
某规范人物标识在当前数据版本中不存在的读取结果。检索底座不区分该标识从未存在还是业已消失,批量读取对其余标识仍然成功。
_Avoid_: 暂时不可用、已删除、404、部分错误

**检索命中(Search hit)**:
检索底座在当前数据版本下对硬条件与研究主题查询返回的一条规范人物结果,含当前字段、命中论文与语义相关性信号,不含完整联系方式,不是匹配运行中的评分结果。
_Avoid_: 匹配结果、候选人、搜索结果

**人物证据(Person evidence)**:
当前数据版本下属于某规范人物的论文与结构化字段出处主张,不依赖某次检索,不含完整联系方式,也不是职位需求解析中的来源摘录。
_Avoid_: 命中证据、来源证据、匹配解释、论文列表

**华人身份(Chinese identity)**:
检索底座根据姓名与国家信息推断的国内华人、海外华人或外国人分类,不是本人声明的国籍或民族事实。
_Avoid_: 国籍、民族、华人血统

**LLM 配置版本(LLM configuration version)**:
平台管理员在线提交并通过最小能力探测后立即启用的不可变模型、参数、隐私路由和提示词组合,新任务使用当前版本。
_Avoid_: 模型环境变量、临时模型配置

**LLM 配置变更尝试(LLM configuration change attempt)**:
平台管理员提交候选配置后执行最小能力探测并尝试原子启用新配置版本的持久化异步工作单元,可排队、执行、等待重试、取消中或以成功、失败、冲突、已取消终结,不是可编辑配置草稿或独立发布步骤。
_Avoid_: 配置草稿、配置发布单

**提示词版本(Prompt version)**:
与结构化输出契约共同发布的不可变提示词模板版本,由 LLM 配置版本引用。
_Avoid_: 在线提示词、临时提示词

**LLM 调用记录(LLM call record)**:
一次生成模型调用尝试在外部请求前写入的不可覆盖核心记录,结果、用量、成本和原始响应由关联的只增事件或受限密文记录承载。
_Avoid_: 解析任务日志、重试汇总

**职位需求 Schema 升级(Job requirement schema upgrade)**:
具有 jobs:manage 且租户可写的成员对可编辑职位需求草稿显式发起的、从创建时 Schema 版本转换到当前配置提示词兼容 Schema 版本的确定性操作,不调用 LLM 重新解释历史内容。
_Avoid_: 自动迁移、模型重解析

**确定性转换器(Deterministic converter)**:
执行职位需求 Schema 升级的有版本纯函数,输入转换前文档,输出转换后文档与逐项升级映射,同一输入永远产生同一输出。
_Avoid_: 升级脚本、迁移模型

**升级映射(Upgrade mapping)**:
确定性转换器对草稿中每个条目记录的转换事实,标明条目来源、转换方式与是否无损,随升级记录长期保留。
_Avoid_: 转换日志、差异报告

**待解决升级项(Pending upgrade item)**:
升级中无法无损转换、需要成员人工选择丢弃或转为未支持条件的条目,全部解决前阻止确认职位需求版本。
_Avoid_: 转换错误、升级警告

**输入正文清理(Input body purge)**:
对到期且不关联已确认职位需求版本的输入快照,经独立受限维护路径清除原始与修正正文、保留来源标识、哈希、长度与清理事实的维护操作。
_Avoid_: 删除快照、清空历史

**保留边界(Retention boundary)**:
决定哪些职位需求数据长期保留(任务、任务事件、终态草稿、版本、已确认快照正文、调用核心与事件)与哪些正文到期清理的固定规则,普通 API 与 Worker 不能删除长期保留数据。
_Avoid_: 过期策略、数据清理计划

**邀请(Invitation)**:
向邮箱发出的加入租户凭证,凭一次性链接注册成为成员。
_Avoid_: 邀请码、注册链接

**两步验证(MFA)**:
基于 TOTP 的第二因子,可由租户策略强制成员开启。
_Avoid_: 双因素认证、二次验证

**套餐订单(Order)**:
租户对套餐的购买请求,需平台管理员审核后生效。
_Avoid_: 交易、支付单

**平台健康状态(Platform Health)**:
首页展示的 API 及其依赖(PostgreSQL、Redis、对象存储)连通性,只有正常与降级两种语义。
_Avoid_: 系统监控、运行指标

### 设计语言

**强调色(Accent)**:
全站品牌点缀色与正文同为 `#000000`,用于主要操作、链接、焦点与当前位置指示;次要说明用 muted 灰阶。
_Avoid_: 主题色、高亮色、珊瑚橘/陶土橘、灰紫 `#8e8ea0`(已退役为正文/主色)

**功能色(Functional color)**:
表达状态语义的低饱和色(成功/在线为绿,错误/离线/破坏性为红),不承担品牌表达。
_Avoid_: 状态色(泛称)、警示色
