# 共享外壳持续标记平台管理上下文

平台管理与租户页面使用同一 Navbar、token 和页面模板，避免形成第二套视觉系统；但 `/admin` 路由涉及跨租户读取与高权限写操作，必须持续显示低饱和、语义化的“平台管理模式”提示，离开该路由后立即消失。

提示只表达权限与操作边界，不充当装饰强调色。跨租户或高风险写操作继续使用明确文案与 AlertDialog 确认，不依赖颜色作为唯一风险信号。
