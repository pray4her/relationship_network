# 小屏继续使用语义 Table

现有数据列表、审计记录、订单和用量均具有稳定的行列关系，移动端不另行渲染 Card 列表。Table 在窄屏按业务优先级隐藏可从详情页获得的次要列，保留对象名称、状态和主要操作；不能安全隐藏的宽表使用明确 `min-width` 与横向滚动容器。

这一策略避免桌面与移动端两套标记、权限判断和操作状态发生漂移，并保留表格的可访问语义。数字、金额和时间列在所有断点保持稳定对齐与 tabular numerals。
