# 前端应用外壳采用顶部 Navbar

当前租户与平台管理导航只有少量扁平入口，没有需要常驻侧栏承载的复杂分组或嵌套层级。统一应用外壳在桌面使用 64px 顶部 Navbar，在移动端将同一组导航链接折叠为菜单；平台管理入口继续按权限显示，不引入常驻 Sidebar，以保留表格和表单的横向空间并避免两套导航源。

现有 `AccountPanel` 与 `WorkspaceNav` 将被这一外壳替代。导航名称、目标路由和权限条件保持 ADR 0009 确认的合同不变。
